spring:
  application:
    name: spring-ai-rag-gio
  ai:
    openai:
      api_key: ${OPENAI_API_KEY}
      embedding:
        options:
          model: text-embedding-3-small

sfg:
  aiapp:
    vectorStorePath: 'd://vectorstore.json'
    documentsToLoad:
      - classpath:/movies500.csv